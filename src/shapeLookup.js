// Define the shape mappings
const shapeMappings = [
  [['rectangle'], 'triangle'],
  [['rhombus'], 'rectangle'],
  [['rounded_rectangle'], 'circle'],
];

// Export the shape mappings
export default shapeMappings;
